### **Epic: 회원가입 경험 정상화 및 고도화 (수정안)**

**목표:** 사용자가 회원가입 과정에서 겪는 모든 불편함을 제거하고, 명확한 피드백과 유효성 검사를 통해 안정적이고 사용자 친화적인 회원가입 플로우를 완성합니다.

**Task 1: API 응답 처리 고도화**

-   [x] Subtask: API 명세를 재확인하여, 회원가입 실패 시 반환되는 구체적인 에러 메시지 형식(`{ "error": "..." }`)을 파악.
-   [x] Subtask: `AuthRepositoryImpl`에서 API 호출 실패 시, 단순 `Exception`이 아닌 에러 메시지를 포함하는 `CustomException`을 던지도록 수정.
-   [x] Subtask: `SignUpViewModel`이 `CustomException`을 잡아, 실패 원인을 `SignUpState`의 `error` 필드에 저장하도록 로직 수정.
-   [x] Subtask: **(테스트)** `AuthRepositoryImpl` 및 `SignUpViewModel`의 에러 처리 로직에 대한 단위 테스트 수정 및 보강.
-   [x] Subtask: **(테스트)** 전체 테스트(`flutter test`)를 실행하여 변경사항이 다른 기능에 영향을 주지 않는지 확인.

**Task 2: 공용 다이얼로그 컴포넌트 개발 및 문서화**

-   [ ] Subtask: 앱 전반에서 재사용할 수 있는 `CustomAlertDialog` 위젯 생성 (`lib/src/presentation/widget/`). (성공, 에러, 확인 등 다양한 타입 지원)
-   [ ] Subtask: `docs/common_widgets.md` 파일을 생성하고, `CustomAlertDialog`의 사용법과 예시를 명확하게 문서화.
-   [ ] Subtask: **(테스트)** `CustomAlertDialog`가 타입에 따라 올바르게 렌더링되는지 위젯 테스트 작성.
-   [ ] Subtask: **(테스트)** 전체 테스트(`flutter test`)를 실행하여 회귀 오류가 없는지 확인.

**Task 3: 회원가입 상태 관리 및 유효성 검사 강화**

-   [ ] Subtask: `SignUpState`에 이메일, 비밀번호, 닉네임 각각의 유효성 검사 결과 및 에러 메시지를 저장할 필드 추가.
-   [ ] Subtask: `SignUpViewModel`에 실시간 유효성 검사 로직 추가 (이메일 형식, 닉네임 입력 여부, 비밀번호 일치 여부).
-   [ ] Subtask: 닉네임 중복 확인 후, 해당 닉네임을 변경할 수 없도록 상태(`isNicknameFixed`)를 추가하여 UI를 제어.
-   [ ] Subtask: **(테스트)** 강화된 상태 및 유효성 검사 로직에 대한 `SignUpViewModel` 단위 테스트 전면 수정 및 보강.
-   [ ] Subtask: **(테스트)** 전체 테스트(`flutter test`)를 실행하여 회귀 오류가 없는지 확인.

**Task 4: 회원가입 UI/UX 대폭 개선**

-   [ ] Subtask: `SignUpScreen`의 각 `CustomTextField` 아래에 실시간 유효성 검사 에러 메시지를 표시하는 로직 추가.
-   [ ] Subtask: 닉네임 중복 확인 후, 성공 시 닉네임 필드와 확인 버튼을 비활성화하고, 실패 시 `CustomAlertDialog`로 명확한 피드백 제공.
-   [ ] Subtask: 모든 유효성 검사가 통과될 때만 '회원가입' 버튼이 활성화되도록 로직 구현.
-   [ ] Subtask: 회원가입 API 호출 실패 시, `ScaffoldMessenger` 대신 `CustomAlertDialog`를 사용하여 실패 원인을 명확히 표시.
-   [ ] Subtask: 닉네임 중복 확인 후, 입력했던 닉네임이 사라지는 버그 수정.
-   [ ] Subtask: **(테스트)** 개선된 UI/UX 로직(비활성화된 버튼, 에러 메시지 등)을 검증하도록 `SignUpScreen` 위젯 테스트 전면 수정.
-   [ ] Subtask: **(테스트)** 전체 테스트(`flutter test`)를 실행하여 회귀 오류가 없는지 확인.

**Task 5: 회원가입 성공 플로우 수정 및 최종 검증**

-   [ ] Subtask: 회원가입 성공(201) 후 발생하는 오류를 디버깅하고, `LoginScreen`으로 안정적으로 이동하도록 수정.
-   [ ] Subtask: **(테스트)** 회원가입 성공 후 네비게이션 동작을 검증하는 위젯 테스트 추가.
-   [ ] Subtask: **(테스트)** 전체 테스트(`flutter test`)를 실행하여, 모든 회���가입 플로우가 완성된 후에도 프로젝트 전체가 안정적인지 최종 확인.