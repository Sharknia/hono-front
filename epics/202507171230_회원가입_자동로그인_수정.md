### **긴급 Epic: 회원가입 성공 후 자동 로그인 플로우 구현 (수정)**

**목표:** 회원가입 성공 시, 반환되는 성공 메시지를 확인한 후, 즉시 로그인 API를 호출하여 토큰을 받아오는 자동 로그인 플로우를 구현하여 파싱 오류를 해결하고 정상적으로 다음 화면으로 전환합니다.

**Task 1: Domain 및 Infrastructure 계층 수정**

-   [x] Subtask: `AuthRepository`의 `signUp` 메소드 시그니처를 `Future<Token>`에서 `Future<void>`로 변경. (토큰을 반환하지 않으므로)
-   [x] Subtask: `AuthRepositoryImpl`의 `signUp` 메소드를 수정하여, 201 응답 시 `void`를 반환하고, 실패 시에만 예외를 던지도록 변경.
-   [x] Subtask: **(테스트)** `signUp` 관련 단위 테스트를 `Future<void>` 반환 타입에 맞게 ��정.

**Task 2: Application 계층 수정**

-   [x] Subtask: `SignUpUseCase`의 `call` 메소드 시그니처를 `Future<Token>`에서 `Future<void>`로 변경.
-   [x] Subtask: **(테스트)** `SignUpUseCase` 관련 단위 테스트를 `Future<void>` 반환 타입에 맞게 수정.

**Task 3: Presentation 계층 (ViewModel) 수정**

-   [x] Subtask: `SignUpViewModel`의 `signUp` 메소드 로직을 아래와 같이 변경:
    1.  `_signUpUseCase`를 호출하여 회원가입을 시도.
    2.  성공 시, `isLoading` 상태를 `false`로 변경하고 `isSignUpSuccess`를 `true`로 설정. (여기서 상태 변경을 한번 끊어 UI가 반응하도록 함)
-   [x] Subtask: **(테스트)** `SignUpViewModel`의 `signUp` 테스트를 새로운 로직에 맞게 수정.

**Task 4: Presentation 계층 (UI) 수정**

-   [x] Subtask: `SignUpScreen`의 `ref.listen` 콜백에서, `isSignUpSuccess`가 `true`가 되면 **`LoginViewModel`의 `login` 메소드를 호출**하도록 수정.
-   [x] Subtask: `LoginScreen`의 `ref.listen` 콜백에서, 로그인 성공 시 (회원가입 직후든, 일반 로그인든) 다음 화면으로 이동하는 로직을 명확히 함. (이 부분은 이미 구현되었을 수 있으므로 재확인)
-   [x] Subtask: **(테스트)** 회원가입 성공 시 `LoginViewModel.login`이 호출되는지 검증하는 위젯 테스트 추가.

**Task 5: 최종 검증**

-   [x] Subtask: 전체 테스트(`flutter test`)를 실행하여 수정된 플로우가 문제없이 동작하는지 확인.
