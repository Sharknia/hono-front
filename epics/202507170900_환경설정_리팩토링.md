### **긴급 Epic: 환경 설정 리팩토링 및 의존성 주입 개선**

**목표:** 테스트 용이성과 확장성을 확보하기 위해, 환경 변수(.env)에 직접 의존하는 구조를 제거합니다. 대신, 명시적인 설정 객체(`AppConfig`)를 생성하고, 이를 Riverpod의 의존성 주입(DI)을 통해 애플리케이션 전반에 안전하게 제공하는 방식으로 아키텍처를 개선합니다.

**Task 1: 설정 추상화 계층 도입**

-   [x] Subtask: 환경 설정 값을 담을 `AppConfig` 클래스 (`lib/src/core/config.dart`) 생성 (`baseUrl` 필드 포함).
-   [x] Subtask: `AppConfig`를 제공할 `appConfigProvider` 생성. 이 Provider는 기본적으로 에러를 발생시켜, 반드시 `ProviderScope`에서 재정의(override)하여 사용하도록 강제.

**Task 2: 앱 진입점(`main.dart`) 리팩토링**

-   [x] Subtask: `main` 함수에서 `--dart-define` 플래그를 기반으로 올바른 `.env` 파일을 비동기적으로 로드.
-   [x] Subtask: 로드된 환경 변수를 사용하여 `AppConfig` 인스턴스 생성.
-   [x] Subtask: `ProviderScope`의 `overrides` 속성을 사용하여, 생성된 `AppConfig` 인스턴스를 `appConfigProvider`에 명시적으로 주입.

**Task 3: 의존성 구조 리팩토링**

-   [x] Subtask: `auth_provider.dart`의 `authRepositoryProvider`가 `dotenv`를 직접 참조하는 대신, `ref.watch(appConfigProvider)`를 통해 `baseUrl`을 얻도록 수정.

**Task 4: 테스트 환경 리팩토링**

-   [x] Subtask: **`flutter_test_config.dart` 파일 완전 삭제.**
-   [x] Subtask: 모든 테스트 파일에서 `dotenv.load()` 직접 호출 로직 제거.
-   [x] Subtask: `ProviderScope` 또는 `ProviderContainer`를 사용하는 모든 테스트에서, `overrides`를 사용하여 가짜(Mock) `AppConfig`를 `appConfigProvider`에 주입하도록 수정. (예: `appConfigProvider.overrideWithValue(AppConfig(baseUrl: '...'))`)

**Task 5: 최종 검증**

-   [x] Subtask: 전체 테스트(`flutter test`)를 실행하여, 새로운 의존성 주입 구조 하에 모든 테스트가 통과하는지 최종 확인.
-   [x] Subtask: Epic 파일의 모든 Task 완료 상태 업데이트.
