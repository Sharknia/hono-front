### **Epic: 로그인 화면 구현 및 인증 처리**

**Task 1: 프로젝트 구조 설정 (DDD 기반)**
- [x] Subtask: `lib/src` 내에 `presentation`, `application`, `domain`, `infrastructure` 디렉토리 생성.

**Task 2: Domain 계층 정의**
- [x] Subtask: 인증 토큰(`accessToken`, `refreshToken`)을 표현하는 `Token` 값 객체(Value Object) 정의.
- [x] Subtask: 데이터 소스와의 계약인 `AuthRepository` 인터페이스 정의 (`login` 메소드 포함).

**Task 3: Infrastructure 계층 구현 (API 연동)**
- [x] Subtask: `http` 패키지 의존성 추가 (`flutter pub add http`).
- [x] Subtask: `AuthRepository`의 구현체인 `AuthRepositoryImpl` 클래스 파일 생성.
- [x] Subtask: `login` 메소드에 대한 **실패하는 단위 테스트** 작성 (HTTP 클라이언트 Mock 사용).
- [x] Subtask: 테스트를 통과하도록 `AuthRepositoryImpl`의 `login` 메소드 구현 (`/api/auth/login` 호출).

**Task 4: Application 계층 구현 (비즈니스 로직)**
- [ ] Subtask: `LoginUseCase` 클래스 파일 생성.
- [ ] Subtask: `LoginUseCase`에 대한 **실패하는 단위 테스트** 작성 (`AuthRepository` Mock 사용).
- [ ] Subtask: 테스트를 통과하도록 `LoginUseCase` 구현 (��력값 검증, `AuthRepository` 호출 등).

**Task 5: Presentation 계층 구현 (UI & 상태 관리)**
- [ ] Subtask: `flutter_riverpod` 패키지 의존성 추가 (`flutter pub add flutter_riverpod`).
- [ ] Subtask: 로그인 상태(초기, 로딩, 성공, 실패)를 표현하는 `LoginState` 클래스 정의.
- [ ] Subtask: `LoginState`를 관리할 `LoginViewModel` (`Notifier`) 클래스 파일 생성.
- [ ] Subtask: `LoginViewModel`의 로그인 로직에 대한 **실패하는 단위 테스트** 작성 (`LoginUseCase` Mock 사용).
- [ ] Subtask: 테스트를 통과하도록 `LoginViewModel` 구현 (`LoginUseCase` 호출 및 `LoginState` 변경).
- [ ] Subtask: `LoginScreen` 위젯 파일 생성.
- [ ] Subtask: `LoginScreen`에 이메일/비밀번호 `TextField`와 `Button`이 존재하는지 확인하는 **실패하는 위젯 테스트** 작성.
- [ ] Subtask: 테스트를 통과하도록 `LoginScreen` UI 위젯 구성.
- [ ] Subtask: `LoginScreen`과 `LoginViewModel`을 Riverpod Provider를 통해 연결하여 UI와 로직 연동.

**Task 6: 라우팅 및 앱 진입점 설정**
- [ ] Subtask: `main.dart`에서 `LoginScreen`을 앱의 초기 화면으로 설정.
