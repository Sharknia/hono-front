### **긴급 Epic: 회원가입 성공 후 화면 전환 오류 수정**

**목표:** 회원가입 성공 시 발생하는 `TypeError`를 해결하고, 성공 다이얼로그를 닫은 후 `LoginScreen`으로 안정적으로 이동하도록 플로우를 수정합니다.

**Task 1: 다이얼로그 및 내비게이션 로직 리팩토링**

-   [x] Subtask: `CustomAlertDialog`에서 `onConfirm` 콜백 파라미터를 제거하여 위젯의 책임을 명���히 분리.
-   [x] Subtask: `SignUpScreen`의 `ref.listen` 콜백을 `async`로 변경.
-   [x] Subtask: `showCustomDialog`의 `Future`를 `await`하고, 다이얼로그가 닫힌 *후에* `Navigator.pop(context)`를 호출하여 화면을 전환하도록 수정.

**Task 2: 테스트 코드 수정 및 검증**

-   [x] Subtask: `CustomAlertDialog` 위젯 테스트에서 `onConfirm` 관련 테스트 로직 제거.
-   [x] Subtask: `SignUpScreen` 위젯 테스트에서 회원가입 성공 시, 다이얼로그가 나타나고 'OK' 버튼을 누르면 `Navigator.pop`이 호출되는지 검증하는 테스트 추가.
-   [x] Subtask: 전체 테스트(`flutter test`)를 실행하여 수정된 로직이 다른 기능에 영향을 주지 않는지 확인.
