### **Epic: 회원가입 기능 구현**

**목표:** 사용자가 이메일, 비밀번호, 닉네임을 사용하여 신규 계정을 생성하고, 생성된 계정으로 즉시 로그인할 수 있는 완전한 회원가입 플로우를 구현합니다.

**Task 1: Domain 및 Infrastructure 계층 확장**

-   [x] Subtask: API 명세에 따라 `AuthRepository` 인터페이스에 `signUp`, `checkNicknameAvailability`, `checkEmailAvailability` 메소드 시그니처 추가.
-   [x] Subtask: `AuthRepositoryImpl`에 `checkNicknameAvailability`에 대한 **실패하는 단위 테스트** 작성.
-   [x] Subtask: 테스트를 통과하도록 `AuthRepositoryImpl`에 `checkNicknameAvailability` 구현 (`/api/users/check-nickname` 호출).
-   [x] Subtask: `AuthRepositoryImpl`에 `signUp`��� 대한 **실패하는 단위 테스트** 작성.
-   [x] Subtask: 테스트를 통과하도록 `AuthRepositoryImpl`에 `signUp` 구현 (`/api/auth/register` 호출).

**Task 2: Application 계층 (비즈니스 로직) 구현**

-   [x] Subtask: `CheckNicknameUseCase` 클래스 및 **실패하는 단위 테스트** 작성.
-   [x] Subtask: 테스트를 통과하도록 `CheckNicknameUseCase` 구현.
-   [x] Subtask: `SignUpUseCase` 클래스 및 **실패하는 단위 테스트** 작성 (비밀번호 일치 여부 검증 포함).
-   [x] Sub-task: 테스트를 통과하도록 `SignUpUseCase` 구현.

**Task 3: Presentation 계층 (ViewModel 및 상태) 구현**

-   [x] Subtask: 회원가입 상태(초기, 로딩, 성공, 닉네임 중복 등)를 표현하는 `SignUpState` 클래스 (`freezed`) 정의.
-   [x] Subtask: `SignUpState`를 관리할 `SignUpViewModel` (`Notifier`) 클래스 및 **실패하는 단위 테스트** 작성.
-   [x] Subtask: 테스트를 통과하도록 `SignUpViewModel` 구현 (`SignUpUseCase`, `CheckNicknameUseCase` 호출 및 상태 변경).
-   [x] Subtask: `auth_provider.dart`에 `SignUpViewModel`을 제공하는 `Provider` 추가.

**Task 4: Presentation 계층 (UI) 구현**

-   [ ] Subtask: `SignUpScreen` 위젯 파일 생성.
-   [ ] Subtask: `SignUpScreen`에 이메일, 닉네임, 비밀번호, 비밀번호 확인 필드와 닉네임 중복 확인, 회원가입 버튼이 존재하는지 확인하는 **실패하는 위젯 테스트** 작성.
-   [ ] Subtask: 테스트를 통과하도록 `SignUpScreen` UI 위젯 구성 (`CustomTextField`, `CustomButton` 재사용).
-   [ ] Subtask: `SignUpScreen`과 `SignUpViewModel`을 연결하여, 닉네임 중복 확인 로직 및 ��과(성공, 실패)를 UI에 반영.
-   [ ] Subtask: 회원가입 버튼 클릭 시 로직을 수행하고, 상태(로딩, 에러)를 UI에 반영.

**Task 5: 라우팅 및 플로우 연결**

-   [ ] Subtask: `LoginScreen`에 '회원가입' 버튼을 추가하고 `SignUpScreen`으로 이동하는 네비게이션 구현.
-   [ ] Subtask: 회원가입 성공 시, 자동으로 `LoginScreen`으로 돌아가도록 네비게이션 구현.
-   [ ] Subtask: `LoginScreen`의 `ref.listen` 부분에, 회원가입 직후 로그인 성공 시 사용자에게 성공 피드백(ScaffoldMessenger)을 명확히 보여주는 로직 보강.

**Task 6: 최종 검토 및 전체 테스트**

-   [ ] Subtask: 다양한 화면 크기(모바일, 웹)에서 회원가입 페이지 레이아웃이 깨지지 않는지 확인.
-   [ ] Subtask: 전체 테스트(`flutter test`)를 실행하여 모든 기능이 정상 동작하는지 확인.
