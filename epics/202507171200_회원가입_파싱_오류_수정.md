### **긴급 Epic: 회원가입 성공 후 응답 파싱 오류 수정**

**목표:** 회원가입 성공 (201 응답) 후 발생하는 `TypeError`의 근본 원인을 파악하고, 서버 응답을 안정적으로 파싱하여 정상적으로 로그인 화면으로 전환되도록 수정합니다.

**Task 1: 원인 파악을 위한 로깅 및 예외 처리 강화**

-   [x] Subtask: `AuthRepositoryImpl`의 `signUp` 메소드에, 201 응답 수신 시 **응답 본문(response body)을 그대로 출력하는 디버그 로그** 추가.
-   [x] Subtask: `Token.fromJson` 호출을 `try-catch` 블록으로 감싸, JSON 파싱 실패 시 어떤 필드에서 문제가 발생했는지 명시하는 `Exception`을 던지도록 수정.
-   [x] Subtask: **(테스트)** 서버가 비정상적인 JSON(예: 필��� 누락)을 반환했을 때, `AuthRepositoryImpl`이 의미 있는 예외를 던지는지 검증하는 단위 테스트 추가.

**Task 2: 응답 모델 및 로직 수정**

-   [ ] Subtask: (로그 확인 후) 서버의 실제 응답에 맞춰 `Token` 모델 또는 `fromJson` 로직 수정. (예: `json_serializable`의 `@JsonKey` 어노테이션 사용 등)
-   [ ] Subtask: 수정된 모델 및 로직에 맞춰 관련 테스트 코드 업���이트.

**Task 3: 최종 검증**

-   [ ] Subtask: 전체 테스트(`flutter test`)를 실행하여 수정된 로직이 다른 기능에 영향을 주지 않는지 확인.
-   [ ] Subtask: (필요 시) 실제 로컬 서버와 연동하여 회원가입 플로우가 끝까지 정상 동작하는지 확인.
